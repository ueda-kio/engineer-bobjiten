# CLAUDE.md

## 1. プロジェクト概要

フロントエンドエンジニア約7名のオンライン飲み会用レクリエーション「エンジニア縛りボブジテン」の Web アプリ。
カタカナ・英語を使わずにフロントエンド用語を説明するゲームで、アプリは**出題のサポート**に特化する。
MVP は通信同期を行わず、各参加者が自端末で個別に開くスタンドアロン構成。

詳細な仕様は `docs/frontend_bobjiten_requirements.md` を参照すること。

---

## 2. 技術スタック / コマンド

React + Vite + TypeScript（SPA） / Tailwind CSS v4 / Vitest / oxlint + oxfmt / Cloudflare Workers（Static Assets）

**パッケージマネージャは pnpm。** `npm` / `yarn` / `npx` は使わない。

| 用途               | コマンド                                              |
| ------------------ | ----------------------------------------------------- |
| 開発サーバ         | `pnpm dev`                                            |
| ビルド             | `pnpm build`                                          |
| テスト（対象指定） | `pnpm exec vitest run --reporter=dot --bail=1 <path>` |
| Lint（対象指定）   | `pnpm exec oxlint <path>`                             |
| フォーマット       | `pnpm exec oxfmt <path>`                              |
| デプロイ           | `pnpm deploy`                                         |

コマンドの定義はこの表のみ。他の章に再掲しない。

### デプロイ設定

`wrangler.jsonc` の `assets.not_found_handling` は必ず `"single-page-application"` を指定する。SPA のクライアントサイドルーティングが 404 になるため。

---

## 3. ディレクトリ構成

```
src/
  domain/              # React 非依存の純粋関数・型定義
    topic.ts           # Topic 型, Category 型, DIFFICULTIES
    rng.ts             # Rng 型, defaultRng
    pick.ts
    pick.test.ts
  data/
    topics.ts          # お題データ（静的定数）
    topics.test.ts     # データ不変条件のテスト（4.1 参照）
  features/            # 機能単位のディレクトリ（状態遷移フック・画面）
  components/          # 汎用 UI コンポーネント
  setup-tests.ts       # Vitest セットアップ
  App.tsx
  main.tsx
```

- **ビジネスロジックは必ず `domain/` に純粋関数として置く。** React コンポーネント内に計算処理を書かない。
- 状態遷移は custom hook に切り出す。コンポーネントは表示に専念させる。
- **テストは対象と同ディレクトリにコロケーションする。** 命名は `<対象ファイル名>.test.ts(x)`。`__tests__/` ディレクトリは作らない。

---

## 4. ドメイン上の不変条件

**以下は明示的な指示がない限り必ず維持する。破る変更は実装前に人間へ確認する。**

### 4.1. お題データ（`src/data/topics.ts`）

```ts
type Topic = {
  id: string;
  word: string; // カタカナ表記
  difficulty: 1 | 2 | 3;
  category: Category;
  relatedWords: string[]; // 3〜5語
};
```

- `id` と `word` に重複があってはならない。
- **各難易度（1・2・3）に最低1件以上の語が存在しなければならない。** お題ピックは各難易度から1語ずつ抽出するため、いずれかが空だと機能が成立しない。
- `category` は定義済みの値のみ。
- **`relatedWords` に、当該お題の `word` と部分一致する語を含めてはならない。** フロントエンド用語は複合語が支配的（例:「ローカルストレージ」に対する「ストレージ」）であり、完全一致の除外では不十分。ヒントとして成立しなくなる。

以上4点は `src/data/topics.test.ts` で保証する。

> **TODO（人間）:** `topics.test.ts` の実装が完了したら、上記4点の箇条書きを削除し
> 「不変条件は `src/data/topics.test.ts` が定義する。テストを緩める変更をしない」の
> 1行に置き換える。仕様の二重管理を避けるため。

**AI はお題データを自律的に追加・削除・改変しない。** 語彙の選定と難易度付けは人間の責務。変更が必要と判断した場合は内容を提示して承認を得る。

### 4.2. 乱数

```ts
export type Rng = () => number; // [0, 1) を返す
export const defaultRng: Rng = Math.random;
```

- **`domain/` 内で `Math.random()` を直接呼ばない。** 乱数を使う関数は `Rng` を引数で受け取る。
- テストでは `Rng` を注入して結果を固定する。`vi.spyOn(Math, 'random')` によるグローバルモックは使わない。
- `Rng` の契約は `[0, 1)`。`1.0` を返す実装を渡すと配列外参照になるため、テストヘルパで `1` を使わない。

---

## 5. 開発フロー（BDD）

人間は「最終的な振る舞い」に責任を持ち、実装コードの逐次レビューは行わない。実装の正確性は自動テストで担保する。

### 適用範囲

Step 1 から開始するのは以下のみ。

- 純粋関数・ビジネスロジックの新規追加
- 状態遷移の変更
- データ定義の制約変更

**UI 配線・スタイリングはテスト不要。直接実装してよい。**
既存ロジックの軽微な修正・リファクタリングは Step 3 から開始してよい。

### Step 1: テストケースの提案

実装前に、テストする振る舞いを**自然言語のケース一覧**で提示する。いきなりテストコードを書かない。以下を必ず含める。

1. 正常系・異常系のケース一覧（例: `正常系: [条件A] のとき、[結果B] となる`）
2. 境界値（空配列、1件のみ、上限値）の明示的な列挙
3. **意図的にテストしないケースと、その理由**

ケース一覧はチャット上に出力する。中間ファイルは作らない（承認後そのまま `describe` / `it` に落とすため）。

**例外:** 乱数・時刻・外部通信を含む処理では、テスト可能にするための設計方針を1〜2行添えて承認対象に含める。乱数は 4.2 で方針確定済みのため、既存方針に従う限り再提示は不要。

### Step 2: 人間による承認

人間がケース一覧をレビューし、過不足がなければ GO サインを出す。

### Step 3: テストコードの実装

### Step 4: プロダクトコードの実装

テストがグリーンになればタスク完了。

---

## 6. テスト戦略

AI はテストを書きすぎる傾向にあるため、スコープを厳守する。

**必須:** すべてのビジネスロジック、状態遷移、計算処理、データの不変条件。

**禁止:** コンポーネントの**単なる表示確認**テスト（`タイトル「xxx」が表示されていること` / `ボタンのラベルが「yyy」であること` など静的アサーション）。

**許容:**

- ユーザーのインタラクションによって**状態が変化し、その結果 UI が切り替わる**動的な振る舞い
- **表示・非表示そのものが要件である場合の静的テスト**（権限による出し分け、情報の秘匿など）

**分量:** 1つの純粋関数につき原則5ケース以内。超える場合は理由を添える。

**Storybook は導入しない。** 対象ユーザーが既知の7名に限定され、UI カタログを維持する必要性が低いため。コンポーネントテストが必要な場合も Vitest で実行する。

---

## 7. テスト構造

テストコードがそれ自体でシステムの仕様書として機能するよう構造化する。

- `describe` と `it` を階層化し、コンテキストと期待される振る舞いを分離する。
- テストケース名は日本語で、条件と結果が明確に伝わる文章にする。
- **`describe` / `it` / `expect` は必ず `vitest` から明示的に import する。** `globals: true` は有効化していないため、グローバル参照するコードは動作しない。

```ts
import { describe, it, expect } from "vitest";

describe("対象の機能やモジュール名", () => {
  describe("正常系", () => {
    describe("〇〇の状態のとき", () => {
      it("XXX という値が返ること", () => {
        // Assert
      });
    });
  });
});
```

> **TODO（人間）:** 実テストファイルが1つできたら、上記コード例を削除し
> 「`src/domain/pick.test.ts` の構造に倣う」に置き換える。実コードとの乖離を避けるため。

---

## 8. テスト・Lint の実行ルール

コンテキスト肥大化を防ぐため、実行時は以下を守る。コマンドは2章の表を使う。

- **テストの全件実行は禁止。** 変更したモジュールのパスを必ず指定する。
- 成功時は結果を再掲せず `pass` とだけ報告する。
- 失敗時はエラーの要点（ファイル・行・原因）のみ抜粋する。スタックトレース全文を貼らない。
- **3回連続で失敗した場合は自力修正を中止し、原因の仮説を提示して人間に判断を仰ぐ。**
- コードの整形は `oxfmt` に任せる。インデント・引用符・セミコロン・行幅を手動で調整しない。整形差分のみのコミットも作らない（pre-commit で自動適用される）。

---

## 9. やらないこと

他章に記載のない禁止事項のみを挙げる。

- **依存パッケージの無断追加。** 理由とともに提案し、承認を得る。
- **設定ファイルの先回り作成。** oxfmt はデフォルト設定で運用する（`.oxfmtrc.jsonc` は作らない）。必要になった時点で人間が判断する。
- Prettier / ESLint / Biome の導入。lint は oxlint、format は oxfmt に統一する。
- `git commit --no-verify` / `git push --no-verify` の使用。pre-commit（lint・format）と pre-push（型チェック・テスト）は必ず通す。
- **MVP スコープ外の機能の先回り実装。** 得点管理・終了条件・リアルタイム同期はフェーズ2。「将来のため」の抽象化も行わない。
- `localStorage` 以外の永続化手段の導入。MVP はバックエンドを持たない。

---

## 10. Git

- コミットは論理的な単位で分ける。テストとその実装は同一コミットにまとめる。
- コミットメッセージは Conventional Commits 形式（`feat:` / `fix:` / `refactor:` / `test:` / `chore:`）。本文は日本語でよい。
- 実装の作業ログをファイルに出力しないため、コミットメッセージ自体が作業ログとなるようなコミットメッセージを作成すること。
- `main` へ直接コミットしてよい。ブランチは人間が明示的に指示した場合のみ作成する。
