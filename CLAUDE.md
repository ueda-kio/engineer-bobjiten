# CLAUDE.md

## 1. プロジェクト概要

フロントエンドエンジニア約7名のオンライン飲み会用レクリエーション「エンジニア縛りボブジテン」の Web アプリ。
カタカナ・英語を使わずにフロントエンド用語を説明するゲームで、アプリは**出題のサポート**に特化する。
MVP は通信同期を行わず、各参加者が自端末で個別に開くスタンドアロン構成。

詳細な仕様は `docs/frontend_bobjiten_requirements_v2.md` を参照すること。

---

## 2. 技術スタック / コマンド

### スタック

* React + Vite + TypeScript（SPA）
* Tailwind CSS
* Vitest（テスト）
* Cloudflare Workers（Static Assets）でホスティング

### コマンド

| 用途 | コマンド |
| --- | --- |
| 開発サーバ | `npm run dev` |
| ビルド | `npm run build` |
| テスト（対象指定） | `npx vitest run --reporter=dot --bail=1 <path>` |
| Lint（対象指定） | `npx eslint --quiet <path>` |
| デプロイ | `npx wrangler deploy` |

### デプロイ設定の注意

`wrangler.jsonc` の `assets.not_found_handling` は必ず `"single-page-application"` を指定する。SPA のクライアントサイドルーティングが 404 になるため。

---

## 3. ディレクトリ構成

```
src/
  domain/                # React 非依存の純粋関数・型定義
    topic.ts             # Topic 型, Category 型, DIFFICULTIES
    rng.ts               # Rng 型, defaultRng
    pick.ts
    pick.test.ts
  data/
    topics.ts            # お題データ（静的定数）
    topics.test.ts       # データ不変条件のテスト（4章参照）
  features/
    game/
      useGame.ts         # ゲーム状態遷移
      useGame.test.ts
      GameScreen.tsx
  components/            # 汎用 UI コンポーネント
  setup-tests.ts         # Vitest セットアップ
  App.tsx
  main.tsxs
```

### 配置ルール

* **ビジネスロジックは必ず `domain/` に純粋関数として置く。** React コンポーネント内に計算処理を書かない。
* 状態遷移は custom hook（`useGame.ts` 等）に切り出す。コンポーネントは表示に専念させる。
* テストは対象と同階層の `__tests__/` に置く。

---

## 4. ドメイン上の不変条件

**以下は明示的な指示がない限り必ず維持すること。破る変更は提案の前に人間へ確認する。**

### 4.1. お題データ（`src/data/topics.ts`）

```ts
type Topic = {
  id: string;
  word: string;              // カタカナ表記
  difficulty: 1 | 2 | 3;
  category: Category;
  relatedWords: string[];    // 3〜5語
};
```

* `id` と `word` に重複があってはならない。
* **各難易度（1・2・3）に最低1件以上の語が存在しなければならない。** お題ピックは各難易度から1語ずつ抽出するため、いずれかが空だと機能が成立しない。
* `category` は定義済みの値のみ。
* **`relatedWords` に、当該お題の `word` と部分一致する語を含めてはならない。** フロントエンド用語は複合語が支配的（例:「ローカルストレージ」に対する「ストレージ」）であり、完全一致の除外では不十分。ヒントとして成立しなくなる。

以上4点はすべて `src/data/__tests__/topics.test.ts` で保証する。

### 4.2. お題データの生成・改変

**AI はお題データを自律的に追加・削除・改変しない。** 語彙の選定と難易度付けは人間の責務。データ変更が必要と判断した場合は、変更内容を提示して承認を得ること。

### 4.3. 乱数

```ts
export type Rng = () => number;   // [0, 1) を返す
export const defaultRng: Rng = Math.random;
```

* **`domain/` 内で `Math.random()` を直接呼ばない。** 乱数を使う関数は `Rng` を引数で受け取る。
* テストでは `Rng` を注入して結果を固定する。`vi.spyOn(Math, 'random')` によるグローバルモックは使わない。
* `Rng` の契約は **`[0, 1)`**。`1.0` を返す実装を渡すと配列外参照になる。テストヘルパで `1` を使わないこと。

### 4.4. フェーズ2の役割別ビュー（実装時）

リアルタイム同期を実装する段階では、**出題者の画面にのみお題が表示され、他の参加者の画面にはマスクされる**ことが最重要要件。サーバから全参加者へお題文字列を送出してクライアント側で隠す実装は不可（開発者ツールで見える）。

---

## 5. 開発フロー（BDD）

人間の開発者は「最終的な振る舞い」に責任を持ち、実装コードの逐次レビューは行わない。実装の正確性は自動テストで担保する。

### 適用範囲

以下に該当する場合のみ Step 1 から開始する。

* 純粋関数・ビジネスロジックの新規追加
* 状態遷移の変更
* データ定義の制約変更

**UI 配線・スタイリングはテスト不要。Step 1〜3 を省略し、直接実装してよい。**
既存ロジックの軽微な修正・リファクタリングは Step 3 から開始してよい。

### Step 1: テストケースの提案（事前レビュー）

実装前に、テストする振る舞いを**自然言語のケース一覧**で提示する。いきなりテストコードを書かない。

提案には以下を必ず含める。

1. 正常系・異常系のケース一覧
   * 例: `正常系: [条件A] のとき、[結果B] となる`
   * 例: `異常系: [条件C] が欠落しているとき、[結果D] となる`
2. 境界値（空配列、1件のみ、上限値）の明示的な列挙
3. **意図的にテストしないケースと、その理由**

ケース一覧はチャット上に出力する。中間ファイルは作らない（承認後そのまま `describe` / `it` に落とすため）。

### Step 1 の例外: テスタビリティに設計判断を要する場合

乱数・時刻・外部通信を含む処理では、ケース一覧に加えて**テスト可能にするための設計方針を1〜2行**で提示し、人間の承認対象に含める。

（乱数については 4.3 で方針を確定済みのため、既存方針に従う限り再提示は不要。）

### Step 2: 人間によるレビューと承認

人間がケース一覧をレビューし、過不足がなければ GO サインを出す。

### Step 3: テストコードの実装

承認されたケースに基づきテストコードを実装する。

### Step 4: プロダクトコードの実装

テストをパスさせるプロダクトコードを実装する。テストがグリーンになればタスク完了。

---

## 6. テスト戦略

AI はテストコードを書きすぎる傾向にあるため、以下のスコープを厳守する。

### 必須

* すべてのビジネスロジック、状態遷移、計算処理
* データの不変条件（4.1）

### 禁止

* コンポーネントの**単なる表示確認**テスト
  * 例: `タイトル「xxx」が表示されていること` / `ボタンのラベルが「yyy」であること`
* 上記のような静的アサーションは一切書かない。

### 許容

* ユーザーのインタラクション（クリック等）によって**状態が変化し、その結果 UI が切り替わる**動的な振る舞いのテスト
* **表示・非表示そのものが要件である場合の静的テスト**
  * 権限による出し分け、情報の秘匿など（例: 4.4 の役割別ビュー、出題者以外にお題が表示されないこと）

### 分量の目安

1つの純粋関数につき**原則5ケース以内**。超える場合は理由を添えること。

### デザインシステム

**Storybook は導入しない。** 対象ユーザーが既知の7名に限定され、UI カタログを維持する必要性が低いため。コンポーネントテストが必要な場合はすべて Vitest で実行する。

---

## 7. テスト構造（動作するドキュメント）

テストコードがそれ自体でシステムの仕様書として機能するよう構造化する。

* `describe` と `it` を階層化し、コンテキストと期待される振る舞いを分離する。
* テストケース名は日本語で、条件と結果が明確に伝わる文章にする。

```typescript
import { describe, it, expect } from 'vitest'

describe('対象の機能やモジュール名', () => {
  describe('正常系', () => {
    describe('〇〇の状態のとき', () => {
      it('XXX という値が返ること', () => {
        // Assert
      })
    })
  })
})
```

* `describe` / `it` / `expect` は必ず `vitest` から明示的に import する。Vitest の `globals: true` は有効化していないため、グローバル変数として参照する。コードは動作しない。

---

## 8. テスト・Lint の実行

出力によるコンテキスト肥大化を防ぐため、**以下の形式でのみ実行を許可する**。

* テスト: `npx vitest run --reporter=dot --bail=1 <対象パス>`
  * **全件実行は禁止。** 変更したモジュールのパスを必ず指定する。
  * `--reporter=dot` により成功時の出力はほぼゼロになる。
* Lint: `npx eslint --quiet <対象パス>`

### 報告と中断のルール

* 成功時は結果を再掲しない。`pass` とだけ報告する。
* 失敗時は、エラーの要点（ファイル・行・原因）のみ抜粋して報告する。スタックトレース全文を貼らない。
* **3回連続で失敗した場合は自力修正を中止し、原因の仮説を提示して人間に判断を仰ぐ。**

---

## 9. やらないこと

* **依存パッケージの無断追加。** 追加が必要な場合は理由とともに提案し、承認を得る。
* **お題データの無断生成・改変**（4.2）。
* `domain/` 内での `Math.random()` の直接呼び出し（4.3）。
* Storybook の導入（6章）。
* テストの全件実行（8章）。
* MVP スコープ外の機能の先回り実装。得点管理・終了条件・リアルタイム同期はフェーズ2。「将来のため」の抽象化も行わない。
* `localStorage` 以外の永続化手段の導入。MVP はバックエンドを持たない。
* テストでの Vitest グローバル API への依存（`globals: true` は使わない）。
* `npm` / `yarn` コマンドの使用。パッケージマネージャは **pnpm** に統一する。

---

## 10. 補足

出力スタイル（応答の簡潔さ等）に関する設定は、プロジェクト横断の個人設定のため `~/.claude/CLAUDE.md` に置く。本ファイルには含めない。
